name: micha_core
description: Extensions and widgets that are missing in Flutter's SDK.
version: 0.3.3
repository: https://github.com/michasng/micha_core
environment:
  sdk: ">=3.1.5 <4.0.0"
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
